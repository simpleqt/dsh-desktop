enableScripts: false
nmHoistingLimits: workspaces
nodeLinker: node-modules

# Universal macOS and dual-architecture Linux releases need both CPU variants
# in the physical node_modules tree before Electron Builder assembles packages.
supportedArchitectures:
  os:
    - current
  cpu:
    - current
    - x64
    - arm64
